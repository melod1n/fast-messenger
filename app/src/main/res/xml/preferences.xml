<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <PreferenceCategory
        android:key="updates"
        android:title="Updates" />

    <PreferenceScreen
        android:key="updates_check_updates"
        android:title="Check updates"
        tools:summary="Current version: 1.4.4" />

    <PreferenceCategory
        android:key="acra"
        android:title="ACRA Crash Reporter" />

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="acra.enable"
        android:title="Enable ACRA" />

    <CheckBoxPreference
        android:defaultValue="false"
        android:key="acra.alwaysaccept"
        android:summaryOff="You need to confirm send report"
        android:summaryOn="Report will be sent without confirmation"
        android:title="Automatically send crash reports" />

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="acra.deviceid.enable"
        android:summary="ACRA will include Device ID (IMEI for GSM and the MEID or ESN for CDMA phones) to report"
        android:title="Include Device Unique ID" />

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="acra.syslog.enable"
        android:summary="Including logcat extracts in reports is a great tool for developers, but it can lead to privacy issues as some other applications might log private data like user account names, opened URLs, calendar events..."
        android:title="Include system logs" />

    <PreferenceCategory
        android:key="misc"
        android:title="Miscellaneous" />

    <PreferenceScreen
        android:key="misc_perform_crash"
        android:summary="App will be crashed. Obviously"
        android:title="Perform crash" />

</PreferenceScreen>